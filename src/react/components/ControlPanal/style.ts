import styled from "styled-components";

export const StyledControlPanal = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
`;

export const RemainTurnText = styled.p`
  position: absolute;
  top: min(2vw, 6vh);
  right: min(5vw, 15vh);
  font-size: min(2vw, 6vh);
  font-weight: bold;
  color: black;
`;
